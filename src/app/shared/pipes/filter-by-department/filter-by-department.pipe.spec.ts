import { FilterByDepartmentPipe } from './filter-by-department.pipe';

describe('FilterByDepartmentPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterByDepartmentPipe();
    expect(pipe).toBeTruthy();
  });
});
